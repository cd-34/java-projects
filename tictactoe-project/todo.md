Not sure what the best way is to keep track of things I need to fix/do
I've seen people use https://trello.com/ for commission related work
For now I'll keep it here

* Remove recursion because the stack heap could potentially get too big and I'd run out of memory
    * nameScanner()
    * moveScanner()  
    * playerAgain();
* Create a makefile command(?) that runs the game in a docker container and put it in the readme
* Look up "pull request" and "git branch" for next changelist 